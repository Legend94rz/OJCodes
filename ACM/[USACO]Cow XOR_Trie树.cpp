/*
ID: rz109291
PROG: cowxor
LANG: C++
*/
#include <iostream>
#include <cstdio>
#include <algorithm>
using namespace std;

int N;
int a[100001];

int main()
{
	//freopen("cowxor.in", "r", stdin); freopen("cowxor.out", "w", stdout);
	cin >> N;
	for (int i = 1; i <= N; i++)
		cin >> a[i];


	fclose(stdin); fclose(stdout);
}